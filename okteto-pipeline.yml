deploy:
    - okteto pipeline deploy --name="test" --skip-if-exists --repository="git@github.com:okteto/movies.git" --branch=main

    - helm repo update
    - |
        cd test &&\
        echo hola &&\
        echo $PATATA
