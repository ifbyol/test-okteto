deploy:
  #remote: true
  commands:
    - name: List identities
      command: eval "$(ssh-agent -s)" && ssh-add -L
    - name: Sleep 6 minute
      command: sleep 360
    - name: List identities
      command: eval "$(ssh-agent -s)" && ssh-add -L
