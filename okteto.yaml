build:
  service-a:
    context: .
    args:
      TEST_VALUE: ${TEST_VALUE}

deploy:
#  remote: true
  commands:
#    - name: Cue
#      command: cue version
    - name: PWD
      command: pwd
    - name: Say hi!
      command: echo hi!
    - name: Helm version
      command: helm version
    - name: Print service-a build tag
      command: |
        echo "IMAGE=$OKTETO_BUILD_SERVICE_A_IMAGE"
        echo "TAG=$OKTETO_BUILD_SERVICE_A_TAG"
        echo "SHA=$OKTETO_BUILD_SERVICE_A_SHA"
#    - name: Ssh list identities
#      command: ssh-add -L
#    - name: Clone private repository
#      command: git clone git@github.com:ifbyol/test-up-statefulset.git
#    - name: Sleep 2 minutes
#      command: sleep 120

#destroy:
#  - name: print something
#    command: echo "hi! this is a test"
#  - name: ls
#    command: ls -la
#  - name: sleep
#  - command: sleep 30
